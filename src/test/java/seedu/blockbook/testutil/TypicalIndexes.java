package seedu.blockbook.testutil;

import seedu.blockbook.commons.core.index.Index;

/**
 * A utility class containing a list of {@code Index} objects to be used in tests.
 */
public class TypicalIndexes {
    public static final Index INDEX_FIRST_GAMER = Index.fromOneBased(1);
    public static final Index INDEX_SECOND_GAMER = Index.fromOneBased(2);
    public static final Index INDEX_THIRD_GAMER = Index.fromOneBased(3);
}

